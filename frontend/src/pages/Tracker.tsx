import ModulePlaceholder from "../components/ModulePlaceholder";

export default function Tracker() {
  return (
    <ModulePlaceholder
      eyebrow="Module 3"
      title="Internships & Certifications"
      description="Live-fetched listings you can save and track, with expiry reminders and semantic search matched against your resume — not just keyword search."
      phaseNote="Ships in Phase 4: Playwright scraper on a schedule, dual-written to Postgres and ChromaDB for RAG search."
      ringSublabel="Match score"
    />
  );
}
